# Dev: Vite proxies /api and /kt to this URL (default port 8843).
# VITE_API_PROXY=http://127.0.0.1:8843

# Vercel production: set in Vercel project env, then redeploy (public FastAPI URL).
# VITE_API_PROXY=https://your-api.example.com
